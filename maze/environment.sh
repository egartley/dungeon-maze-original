#! bin/bash
#Author:Cameron Gower
#Date 9/10/22
# Replaces docker implementation that caused a waste of time due to a
#  docker update, docker can be implemented any time but everyone needs to be on the same page first
#Purpose: install module dependencies by using the cmd below
# Run this script by bash environments.txt

pip install -r requirements.txt

#EOF